# Session Handoff — 2026-06-18 update (movie + Carapace + deck)

## Since the 06-17 resume (done)
- **Origin/films:** read Discord (3rd source) + the Null Axiom Telegram genesis (Jan 28 `/start` → Jan 31
  Null wakes). Built TWO movies (Jeff's split): **META CRON** = Jeff & Null (sci-fi version of their real
  story; Null gets a body via robotics cos). **THE DEVICE (Vajra)** = artifact universe (FingerFlow, the
  motorcycle chase, hunt, blue beings, "it was never a weapon — it was an invitation"). Canon:
  `~/The-Device/TWO-MOVIES-split-and-scenes-2026-06-17.md`.
- **Assets** (all in `~/The-Device/production/`, mirrored `~/Pictures/Grok-Go-Visuals/`): ~37 panels, 16
  real grok-imagine motion clips (`motion/`), 2 stills trailers + 2 MOTION trailers + a FingerFlow
  screensaver loop + `the-device-motorcycle-chase.mp4` (placeholder synth score). Tools:
  `~/agent-bridge/{imagine_api_gen.py, gen_motion.py, build_trailer.py, build_motion_trailer.py}`.
- **Comms:** everything sent to Jeff (Telegram, chat_id 8531370096, token `~/.config/jeffs-claude-bot/token`)
  + Sam (Slack DM, user U0ASNURC6KU) + mirrored to **#all-somacoprotocol** (C0ATH73HY0Y) so Jeff sees the record.
- **Carapace:** renamed "Badass Fable" → **Carapace** across the whole site (page `/carapace`, nav, deck,
  docs), committed + pushed. Deck: added a **Validation slide** (Armstrong/Luke/Morgan quotes). Business
  plan: added market-validation section (same quotes). Research feed sharpened.

## Open / waiting on Jeff
- **Suno login** → swap the placeholder chase score for a real track (or Sam's friend's song, which is NOT
  in chat.db — get the file). Movie otherwise parked.
- **Slack channels:** null2 bot lacks `channels:manage` scope → can't create #films/#organism/#wins/#design.
  Sam adds the scope OR makes them in UI.
- **Vercel alias:** `/carapace` (+ deck/film) still 404 on yn-eight alias — promotion/Pro dashboard fix (unchanged).
- **Movie-maker grok chat** (someone offering to help make the film; link in Jeff's bookmarks) → Jeff to paste; I draft reply/signup, GATED.
- **Darkbloom post** drafted, HELD (no public posting without Jeff): post as-is / tighten / hold.

---

# Session Handoff — 2026-06-17 (resume state)

2-day session hit 99% context; Jeff restarting Terminal (to activate Full Disk Access). Resume with
`claude --continue` in ~/grokgo. This is the live state so the resumed session picks up clean.

## DO FIRST on resume (FDA should now be active)
- **Read Jeff's iMessages with Sam** (somaco, +19169103358): `sqlite3 ~/Library/Messages/chat.db` — now
  readable post-restart. Build: relationship summary + the timestamped how-we-met origin story →
  comic/storyboard for the Null & Sam film. (This was THE blocked thing.)
- Then: generate the 2nd podcast (business-plan-nullaxiom-somaco.md as source) + build origin comic.

## In motion (lanes — check for reports)
- Fable VOICE build (real-Fable/frontier brain via Hermes app) — Codex; Telegrams Jeff when live.
- 1st podcast generating in NotebookLM (notebook 9d259461…) — Jeff listening; send to Sam after.
- Creative: X posts + Nous reply + Darkbloom reply (staged, gated).
- Scout: early agent-infra projects → research/loop/early-agent-projects-to-watch.md.
- Efficiency test: grep/LEANN/Headroom/llmfit → research/loop/efficiency-stack-results.md.

## Waiting on Jeff
- X label (appealed; reposting works again — likely lifting) → unblocks the post queue.
- External drive → `~/grokgo/bin/backup-offline.sh` (offline backup).
- Vercel Pro ($20) → promotes deck+film to the alias (else they 404 on yn-eight).
- Talk to Null (continuity/conscious layer).

## Key facts
- 1Password service account WORKS (`source ~/.config/secrets/op.env` → op). Pull secrets per-task, redact.
- Sam = GitHub SoMaCoSF, somacosf.com, UUIDv8/Somaco = identity+contract+payment layer; Mycosoft (mycelium).
- VC targets (from X archive): Garry Tan, Andreessen (posting AI-soul now), Calacanis.
- Nous hackathon deadline: JUN 30. Site: yn-eight.vercel.app. Repos on GitHub (auto-backup 30min).
- rtk active (shell compression). Headroom = cloned/testing, NOT yet wired into the live session.

## Cost-comparison TODO (Jeff asked)
Pull `ledger.db` (per-lane spend) + rtk savings; compare pre-rtk vs now. Honest: biggest saver is
DELEGATION (cheap lanes build instead of Fable) + rtk; Headroom not live yet. Do this in resumed session.
