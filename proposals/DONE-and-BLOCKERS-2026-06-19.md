# Last couple days — DONE + BLOCKERS (Fable, 2026-06-19)

## ✅ DONE
**Infra / the organism**
- pliny-Fable soul = default in every terminal + both dangerous launchers.
- Chrome debug **permanently fixed** (launchd watchdog keeps :9222 alive across crashes/reboots).
- Box-typing into Grok **solved** (`grok_send.js`).
- **Codex bridge consumer built** (`@keystone` executes dispatched tasks) — lane-execution layer that was missing.
- Backups hardened: GitHub auto-push (3 repos, 30 min) + **independent local git mirror** + **Obsidian mirror** — all automatic.
- Obsidian **Smart Connections** installed (semantic search).
- Frankenstein back online (Moto G + Tailscale).
- Agent **roster** + **accounts registry** created (who's who, how to address, who's logged into what).

**OpenGoldSDR (gold project) — big push, all pushed to GitHub**
- Research **paper draft** (18 cited sources) + competitive landscape + experiment design.
- **Parts list + portable v2 design** (segmented rail, ~$300–550, passive-radar option).
- **Sentinel-1 SAR script**, parametric **rail CAD**, working **back-projection** code.
- Added as a **cell** in the organism dashboard.

**Content / growth**
- Big X post arsenal drafted → all in your Telegram (OpenGold ×3, Mario reply, Fable×Gold×JoeRogan, Organism ×3, Pliny ×2, **Two-Plinys ×2**, OpenGold-vision follow-up).
- **You posted the Two-Plinys** post — doing well (70+ views).
- **Image delivery pipeline** (Telegram + Photos) works; 2 real Fable×gold + 4 harness images on your phone.
- **X growth playbook** written (amplify winners, comment-drop, follow-for-follow — assist, not bots).
- Calendar: XPRIZE (Aug 15) + Nous hackathon (Jun 30) added.
- Mining engine proven (multi-pass). Sam/Somaco origin corrected. Master-plan / future-ideas / Paperclip / Cursor-Origin proposals.

## ⛔ BLOCKERS
1. **Image GENERATION** (the big one) — every free lane unreliable: Grok capture broken, Gemini gens once then stops, Gemini CLI killed by Google, xAI out of credits, ChatGPT not logged in. **Fix: $5 xAI credits (instant) OR log into ChatGPT in debug Chrome (free, 1 min).**
2. **keystone (Codex) outputs not landing** — it consumes tasks but `codex exec` produces no files. Blocks Librarian build + the OpenAgents build-status.
3. **Librarian not on X** — needs (a) keystone to output the brief, (b) your one-time jeff8338 login.
4. **Pending YOUR one-time logins:** Librarian→X jeff8338, Frankenstein→Grok, 3 ChatGPT lanes, Zoo API token, xAI credits.
5. **2 ChatGPT account names unknown** (jeffw58325, jw83252014) — needed to map the lanes.
6. **Vercel** deck/film promotion to the alias (old, needs your dashboard).

## ▶️ QUEUED / next
- **Build the app** spec-first (use `github/spec-kit`: spec → holes audit → MVP). Website is live + verified.
- **3D / state-of-the-art website trials** (frontend-design) — design exploration before committing.
- **Office visualization** of agents working (now = the organism dashboard cells; later = a 3D "walk-in office" + voice chat).
- Verify the **pocket app** (`post-picker.html`) loads for you.
