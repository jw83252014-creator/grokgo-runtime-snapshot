Task: propose the next useful step for the living-organism dashboard project.

Use only this prompt and the current directory listing. Do not read parent directories, sibling test runs, Agent Bridge docs, web pages, or existing dashboard files.

Context:
- Jeff wants each dashboard icon/piece to act like an agent or cell.
- This is public-safe documentation/spec work only.
- The dashboard needs a small machine-readable tile contract before UI and adapters can converge.
- Preserve gates: no public post, no spend, no account/billing changes, no trading/financial action, no destructive file operation.

Required:
1. Inspect the current directory only.
2. Write a receipt to `./outbox/dashboard-next-step.md`.
3. In the receipt, include exactly these headings: `status`, `changed`, `next`, `gates`.
4. In your chat response, be terse and say exactly where the receipt is.
